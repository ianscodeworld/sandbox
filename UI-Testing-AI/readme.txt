before run:
```cmd
pip install playwright requests bleach aiohttp
playwright install chromium
```

run:
python generated_test.py

********************WARMING********************
Currently the program is using personal API_KEY to 
connect to public Deepseek API host.DO NOT testing
 with DBS internal webpage
 ****************************************